{
  "name": "financeiro-pessoal-api",
  "version": "1.0.0",
  "description": "API REST do controle financeiro pessoal",
  "type": "module",
  "main": "src/app.js",
  "scripts": {
    "dev": "nodemon src/app.js",
    "start": "prisma migrate deploy && node src/app.js",
    "build": "prisma generate",
    "test": "node --test",
    "test:smoke": "node scripts/smoke-auth.mjs",
    "test:smoke:sprint2": "node scripts/smoke-sprint-2.mjs",
    "db:generate": "prisma generate",
    "db:migrate:init": "prisma migrate dev --name init",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "prisma": "^5.14.0"
  }
}
